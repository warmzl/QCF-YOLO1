# Ultralytics YOLO 🚀, AGPL-3.0 license
# COCO128 dataset https://www.kaggle.com/ultralytics/coco128 (first 128 images from COCO train2017) by Ultralytics
# Documentation: https://docs.ultralytics.com/datasets/detect/coco/
# Example usage: yolo train data=111.yaml
# parent
# ├── ultralytics
# └── datasets
#     └── coco128  ← downloads here (7 MB)

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /home/gzz/zhoulin/model-sound code/yolov8/mydatas # dataset root dir
train: mydatas/train.txt # train images (relative to 'path') 128 images
val: mydatas/val.txt # val images (relative to 'path') 128 images
test: # test images (optional)

# Classes
names:
    0: missing_hole
    1: mouse_bite
    2: open_circuit
    3: short
    4: spurious_copper
    5: spur
